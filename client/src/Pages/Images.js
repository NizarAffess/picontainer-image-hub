import { Container, Typography } from "@mui/material";

const Images = () => {
  return (
    <Container>
      <Typography variant="h3">Images list</Typography>
    </Container>
  );
};

export default Images;
